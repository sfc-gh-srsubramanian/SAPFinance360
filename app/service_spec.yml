spec:
  containers:
  - name: finance360
    image: /sc360_app_provider/images/repo/finance_360:latest
    env:
      PORT: "8080"
    resources:
      requests:
        memory: 1Gi
        cpu: 500m
      limits:
        memory: 2Gi
        cpu: 1000m
    readinessProbe:
      port: 8080
      path: /healthcheck
  endpoints:
  - name: finance360
    port: 8080
    public: true
serviceRoles:
- name: finance_360_role
  endpoints:
  - finance360
